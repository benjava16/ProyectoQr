specVersion: "v1beta"
serviceId: "AppQr"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "AppQr-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
